// media.manifest.mjs — Pip & Parcel (site-03, children's craft boxes) — TYPE/CSS-ART-LED
// Intentionally EMPTY: this site uses no photography by design. All visuals are type, flat
// color, and CSS/SVG shapes (blobs, wavy seams, sticker cards). Do not sideload anything.
export default [];
